<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android" >
    <PreferenceCategory android:title="栞同期の設定" >
       <EditTextPreference
            android:key="BookmarkSyncHost"
            android:title="ホストIPアドレス"
            android:summary="栞同期サーバーのIPアドレス"
            android:dialogTitle="ホストIPアドレス" />
       <EditTextPreference
            android:key="BookmarkSyncPort"
            android:defaultValue="8081"
            android:title="ポート番号"
            android:summary="栞同期サーバーのポート番号"
            android:dialogTitle="ポート番号" />
       <EditTextPreference
            android:key="BookmarkSyncUser"
            android:title="ユーザー名"
            android:summary="HTTP認証用ユーザー名(任意)"
            android:dialogTitle="ユーザー名" />
       <EditTextPreference
            android:key="BookmarkSyncPass"
            android:title="パスワード"
            android:password="true"
            android:summary="HTTP認証用パスワード(任意)"
            android:dialogTitle="パスワード" />
    </PreferenceCategory>
</PreferenceScreen>
